// Exercício 1: Propriedade length de funções
// Crie uma função chamada soma que receba dois parâmetros e retorne a soma deles. Em seguida, exiba no console quantos parâmetros a função soma espera usando a propriedade length.

function soma(n1, n2) {
  return n1 + n2;
}

console.log(soma.length)
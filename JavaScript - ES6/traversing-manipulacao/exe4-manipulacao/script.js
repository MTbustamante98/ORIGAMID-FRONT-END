const shoppingList = document.getElementById('shopping-list');

// const terceiroItem = shoppingList.children[2];

shoppingList.removeChild(shoppingList.children[2]);
shoppingList.removeChild(shoppingList.children[0]);